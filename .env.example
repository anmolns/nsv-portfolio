# Supabase (public site + admin)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Bulk upload server only (never expose to browser — keep in .env.local)
# Supabase Dashboard → Settings → API → service_role secret
SUPABASE_SERVICE_ROLE_KEY=
